# Server runtime configuration
# Copy to .env and adjust as needed: cp .env.example .env

# development | production (default: development)
ENV=development

# API build version (default: 0.0.0)
VERSION=0.0.0

# Optional: artificial delay for /ping in milliseconds (default: 0)
# PING_DELAY_MS=0
